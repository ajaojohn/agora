// Cross-process types (IPC contract, domain shapes) live in this directory.
// Both tsconfig.node.json and tsconfig.web.json compile this folder so the same
// types are available to main, preload, and renderer.
// Empty for now — commit 5 introduces ipc.ts (channel constants + payload types).
export {};
